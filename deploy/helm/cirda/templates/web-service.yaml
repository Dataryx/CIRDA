apiVersion: v1
kind: Service
metadata:
  name: {{ include "cirda.fullname" . }}-web
  labels:
    {{- include "cirda.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cirda.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
